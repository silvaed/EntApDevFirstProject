@page
@model EntApDevFristProject.Pages.TimeModel
@addTagHelper*, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Time</title>
</head>
<body>
    
    @*<form method="post">
        <fieldset>
            @for(int i = 0;i<Model.DaysChoice.Length;i++)
            {
                <label asp-for=DaysChoice[i]>@Model.DayOfWeek[i]</label>
                <input asp-for=DaysChoice[i] />
            }

        </fieldset>
        <button type="submit">Send The Information</button>
     </form>

    <ul>
        @for(int i = 0; i < Model.DaysChoice.Length;i++)
        {
            if (Model.DaysChoice[i])
            {
                <li>@Model.DayOfWeek[i]</li>
            }
        }
    </ul>*@

    <form method="post">
        <select asp-for=ChoiceMonths asp-items=@Model.Months>

        </select>
        <button>Send the information</button>
    </form>

    <ol>

        @for (int i = 0; i < Model.ChoiceMonths.Length; i++)
        {
            <li>@Model.ChoiceMonths[i]</li>
        }

    </ol>

        












</body>
</html>
